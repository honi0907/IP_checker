---
description: 共通 build / version / release 運用（完了報告前に build 実行など）
alwaysApply: true
---

# Common App Rules

- 変更作業は **完了報告前に対象アプリの build を実行**する
- **起動中ならビルド前にプロセスを強制終了**する（`Stop-Process -Name IPChecker -Force -ErrorAction SilentlyContinue`）
- build 後は成果物名と出力先を確認する
- **リリースビルドでは毎回ポータブル ZIP も生成**する（`.\scripts\Build-Installer.ps1`）
- version はリリースのたびに上げる。同じ tag の再利用はしない
- パッチは **0〜9**。`X.Y.9` の次は **`X.(Y+1).0`**（例: `1.0.9` → `1.1.0`）
- 生成物フォルダに古い成果物を残さない
- 運用ルール変更時は `BUILD_RULES.md` 等も同時更新

詳細: cursor-playbook `docs/COMMON_APP_RULES.md` / 本プロジェクト `BUILD_RULES.md`
